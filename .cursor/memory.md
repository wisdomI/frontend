- Fixed two issues: 1) "Rendered more hooks than during the previous render" error in app/vendor/page.tsx was caused by useCallbacks in useVendorBookings having function dependencies that were recreated on every render. Fixed by removing fetchBookings, fetchMeetings, fetchStats from the useEffect dependency array in useVendorBookings.ts, leaving only [autoFetch]. 2) Vendor dashboard access issue on first login was caused by a race condition where the AuthContext hadn't updated yet when vendor/layout.tsx checked user.accountType. Fixed by making the vendor layout check both user?.accountType from context AND localStorage.getItem('userAccountType') as a fallback, using effectiveAccountType = user?.accountType || storedAccountType. Also added account type validation in LoginForm.tsx to prevent login with wrong toggle selected. (ID: 10963164)
- Addressed security vulnerabilities by removing sensitive console logs (tokens, PII) in app/admin/auth/login/page.tsx, app/admin/auth/signup/page.tsx, contexts/AuthContext.tsx, middleware.ts, and extensively in lib/api.ts. Wrapped debug helpers in development env checks. Sanitized error logging in app/super-admin/admins/create/page.tsx and components/marketplace/MarketplaceDashboard.tsx.